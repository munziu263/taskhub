version: "3.8"
services:
  api:
    stdin_open: true
    tty: true
    build:  
      context: ./api
    ports: 
    - "5000:5000"
    volumes: 
    - ./api:/api/
    environment: 
      FLASK_ENV: development
  frontend:
    stdin_open: true
    tty: true
    build: 
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports: 
      - "3000:3000"
    depends_on: 
      - api
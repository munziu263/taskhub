version: "3.8"
services:
  api:
    stdin_open: true
    tty: true
    build:  
      context: ./api
    ports: 
    - "5000:5000"
    volumes: 
    - ./api:/api/
    environment: 
      FLASK_ENV: production
  frontend:
    stdin_open: true
    tty: true
    build: 
      context: ./frontend
      dockerfile: Dockerfile.prod
    ports: 
      - "8080:80"
    depends_on: 
      - api